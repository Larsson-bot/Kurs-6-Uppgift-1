@page "/logout"


@code {      
    protected override async Task OnInitializedAsync()
    {
        await sessionStorage.RemoveItemAsync("AccessToken");
        NavManager.NavigateTo("/logoutpage", true);
    }
}
